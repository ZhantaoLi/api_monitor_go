services:
  api-monitor-go:
    build: .
    container_name: api-monitor-go
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      - LOG_CLEANUP_ENABLED=1
      - LOG_MAX_SIZE_MB=128
      - API_MONITOR_TOKEN=lming
    volumes:
      - ./data:/app/data
